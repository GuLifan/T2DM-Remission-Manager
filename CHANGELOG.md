# 变更记录（CHANGELOG）

本项目版本号遵循 `vX.Y.Z`：X 为大版本（结构级变更），Y 为功能版本，Z 为修订版本。
V0.1 为历史版本（工作区 `ETMMS_V0.1_dev_260818`，只读保留，不再演进）；V1.0 为当前唯一在研版本。

## [Unreleased] — v1.0.0 开发中

### 新增（M1：工程基线）

- **后端工程**：`backend/` 使用 uv 管理项目内虚拟环境（`.venv`，不改动全局 Python）；Python 版本经实测锁定 **3.14**
- **配置与基础能力**：`config.py`（数据目录/数据库路径/端口/会话时长，便携版写用户数据目录）、`core/clock.py`（时钟注入，保证日期边界可测）、`core/logging.py`（结构化日志）、`core/exceptions.py`（业务异常 → 医生可读自然语言）
- **数据层**：SQLAlchemy 模型（`users` / `patients` / `events` / `audit_log` / `app_meta`）+ **Alembic 迁移**（首个迁移 `7d4fbd959d06`），启动时自动升级；SQLite 启用 WAL 与外键约束
- **事件与审计**：`events` 记录操作者、来源（医生/系统）、源与目标状态、规则 ID、输出文案、输入快照；`audit_log` 记录登录与全部写操作
- **前端工程**：React + TypeScript + Vite 脚手架；`styles/tokens.css` 与 `UI.md` 逐字一致；`styles/components.css` 以 BEM 类名实现全部组件样式
- **组件库（14 个）**：`PageHeader` / `ActionBar` / `SettingsRow` / `FieldRow` / `ChoiceGroup`（互斥分支：单选 + 优先级 + 禁用原因）/ `CheckboxGroup` / `ResultBanner` / `StatusBadge` / `SideNav` / `EvidenceDrawer` / `ConfirmDialog` / `EmptyState` / `ErrorBanner` / `LoadingBlock`
- **工程脚本**：`scripts/dev.py`（一键启动）、`scripts/build.py`（便携版打包流水线）、`scripts/check_docs.py`（Token 定义一致 / Token 使用率 / 模板覆盖自查）
- **测试基线**：后端 pytest 3/3（含外键约束验证）、前端 vitest 5/5（含互斥分支禁用原因）

### 新增（M2：数据与账号）

- **密码与会话安全**（`core/security.py`，仅用标准库）：scrypt 加盐密码哈希（参数随哈希存储，便于日后提高强度）；HMAC-SHA256 签名会话令牌（含签发/过期时间，密钥首次运行生成并落盘，避免重启踢人）
- **登录防护**：连续失败 5 次锁定 15 分钟；账号不存在与密码错误返回**同一句提示**（避免账号枚举）；停用账号拒绝登录
- **账号接口**（`api/auth.py`）：首次运行引导创建医生账号（无默认密码）、登录、登出、当前账号查询
- **患者档案接口**（`api/patients.py`）：建档（病历号唯一预检）、列表、详情、事件流水分页查询
- **数据访问层**（`repository/`）：users / patients / events / audit 四个模块；**事务边界由接口层掌握**，仓储只 add+flush；事件模块刻意不提供更新与删除接口
- **系统保留账号**：`__system__`（不可登录），供系统自动事件记录操作者
- **数据库迁移** `375fb4767e0a`：新增 `users.failed_login_count` 与 `users.locked_until`
- **测试**：新增密码/令牌安全测试、账号接口测试、患者档案与审计测试；后端 pytest 由 3 项增至 **24 项**

### 新增（M3：临床内核）

- **领域层（单一数据源）**：`domain/states.py`（9 状态 + 6 流程单元）、`domain/enums.py`（字段取值）、`domain/templates.py`（30 条锁定文案 + 2 条 SYS 兜底）、`domain/branches.py`（阶段复评分支优先级表）、`domain/transitions.py`（合法转换表 + 校验 + 无出口检查）
- **6 个临床服务（纯函数）**：`pre_assessment` / `full_assessment` / `phase_review` / `observation` / `remission_judge` / `post_remission`；时间由调用方注入，返回前强制校验状态跳转
- **日期与默认值**：`utils/date_utils.py`（日历月加法月末钳位、最早可判定日期 MAX 规则）、`services/defaults.py`（12 周复评间隔与随访节奏的唯一来源）
- **分支优先级落地**：阶段复评按"明显失控 > 治疗下达标用获益药 > 停用最后一种药 > 常规动作"裁决；**被覆盖分支必须返回原因**，互斥分支同时为真直接报错（修复 V0.1"停药日期被静默丢弃"的缺陷）
- **测试**：状态机矩阵与无出口检查、日期边界、六个服务全分支、3 个锁定病例端到端、四条红线回归、锁定文案与甲方原件逐字比对；后端 pytest 由 24 项增至 **133 项**

### 新增（M4：接口层）

- **13 个流程端点 + 3 个领域端点**：6 个流程单元全部接入；新增"重新发起预评估"入口（ST00→ST10）
- **共享骨架** `api/flow_common.py`：患者取用与状态守卫、结果落库、**事件与审计同事务**、幂等守卫
- **幂等**：请求携带 `request_id`，重复提交返回 409 与自然语言提示，不产生重复事件
- **只读语义修正**：缓解判定拆分为 `check`（纯查询，可反复调用、不落库）/ `route`（落库）/ `confirm`（医生确认），修正 V0.1 报告中记为 L5 的"核对接口带副作用"问题
- **领域导出**：`/api/domain/states`、`/flow-units`、`/branches`——前端按后端返回的分支优先级渲染，禁止自行硬编码
- **修复 V0.1 的潜伏缺陷**：其"急性安全稳定化后快速建档"接口跳转 `ST10→ST31`，但该转换不在 V0.1 的状态矩阵中，接口一旦调用即抛非法跳转；V1.0 补齐转换并加测试覆盖
- **测试**：新增接口层测试（主链路、病例3、入口守卫、只读性、幂等、事件与审计同事务）；后端 pytest 由 133 项增至 **146 项**

### 新增（M5：前端成品）

- **页面**：登录页（首次运行同页创建账号）、患者列表与建档、患者工作台（左导航 + 页头 + 内容区 + 底部动作条）、6 个流程页面（预评估 / 完整评估 / 阶段复评 / 观察期 / 缓解判定 / 缓解后复评）+ 常规管理 + 关闭路径
- **接口层**：`api/client.ts`（15 秒超时、AbortSignal 取消、401 自动清理会话、统一自然语言错误、写操作自动带 `request_id`）+ `api/endpoints.ts`（按流程分组的类型化接口）
- **导航规则**：全应用只有一套流程导航（左侧）；未到达环节禁用并说明原因；**已完成环节可点击回看**（只读展示该环节历史结论）
- **分支优先级渲染**：阶段复评的分支选项与顺序来自后端 `/api/domain/branches`（前端不硬编码）；未选中的分支逐条列出"本次未生效"及原因
- **交互修复**（针对 V0.1 缺陷）：控件标签统一由 `FieldRow` 注入 id/aria-describedby；数字 0 不再被吞；未知状态不泄漏状态代码；提交前就地校验；请求超时与取消；按钮禁用 + 幂等防重复提交
- **样式**：新增 `styles/pages.css`，全部取值来自 Token；页面与组件零 inline style
- **测试**：Vitest 4 个文件 10 项用例（组件 5 项 + 登录页 2 项 + 工作台导航 3 项）
- **真实走查**：在浏览器中完成"创建账号 → 建档 → 预评估 → 完整评估 → 阶段复评"全链路验证

### 新增（第二轮第一批：测试可用性）

- **安全测试模式**：新增 `ETMMS_TEST_MODE` 总开关，并以“测试模式 + 测试账号 + 测试患者”三重守卫保护测试跳转；默认关闭，真实患者由后端硬阻断
- **显式测试跳转**：测试患者工作台提供 9 个自然语言状态入口；跳转不弱化正式状态机，独立写入 `source='debug'` 的事件与审计记录
- **账号级日期模拟**：测试账号可设置或清除模拟日期；设置跨刷新、重新登录与服务重启持久保存，修改后返回患者列表，全站持续提示模拟状态
- **临床写入保护**：模拟日期只影响当前测试账号；仅测试患者允许在模拟日期下写入，事件额外记录实际 `simulated_date`
- **数据迁移**：新增迁移 `9c4a2f1b7e10`；精确回填 `admin` 为测试账号及 3 位既有演示患者为测试患者，不对其他患者做模糊识别
- **前端信息层级**：新增 `DateControl`、`TestToolsPanel` 与常驻 `TaskPanel`；任务标题 Token 已有真实使用点，文档门禁由 2/3 恢复为 3/3
- **命名一致性**：完成 `mapping.md` → `MAPPING.md` 的 Git 大小写重命名及脚本、文档引用修正
- **测试与走查**：后端 **152 passed** + ruff；前端 **14 passed / 6 files** + lint + build；在 Chromium 浏览器中核验第 4、5、6 流程页、日期持久化、测试事件标识及真实患者入口隔离
- **走查修复**：患者列表“当前环节”改为读取后端领域状态表，不再用仅适用于主动管理期的 `stage` 字段把 ST40/ST50/ST60 误显示成“常规管理”（`b5b4db4`）

### 新增（第二轮第二批：档案与录入）

- **开放注册**：已有账号后仍可注册医生账号；姓名、选填科室、两次密码由前后端共同校验，注册成功后返回登录而不自动进入系统
- **门诊档案字段**：界面统一使用“住院号”；出生年月分栏录入且日固定为 1；新增当前科室、联系方式、创建者与资料完善标记；69 个科室由后端字典统一输出
- **批量导入**：支持 UTF-8 CSV 与 XLSX（5 MB 上限）；按行报告成功、重复跳过和失败，不覆盖既有患者；导入患者首次使用前必须由医生完善资料
- **结构化评估**：病程改为年/月双框；预评估前移身高体重并计算 BMI 最新快照，完整评估自动带入；BMI 仅记录数值，不作分级或准入判断
- **参考字典**：既往 T2DM 诊断基础改为 6 项单选，降糖药物改为类别多选；两者均支持“其他”补充说明，选项由 `/api/reference` 单一输出
- **通用录入控件**：新增 `NumericInput`、`DateInput`、`YearMonthInput`、`SelectWithOther`、`MultiSelectWithOther`、`ImportDialog`、`PatientProfileForm` 与 `FooterBar`；临床数值固定显示单位，日期文本统一 `yyyy/mm/dd`
- **布局与文案**：内容最大宽度调整为 1120px 并居中；页脚显示用途声明与实际后端版本；常规管理按钮改为“发起60秒缓解预评估”并显示既往记录数
- **数据迁移**：新增 `4f8e1c2d9a70`；开发库 69 科室种子完整，既有 4 名患者均安全回填资料完善与创建者
- **测试与走查**：后端 **156 passed** + ruff；前端 **17 passed / 7 files** + lint + build；浏览器验证导入入口、科室、BMI 即时计算与自动带入、结构化选项、“其他”输入、日期与版本页脚

### 新增（第二轮第三批：流程与权限）

- **正式角色**：首次引导账号固定为 `admin`，开放注册固定为 `doctor`；管理权限与 `is_test_account` 完全分离，旧库无管理员时只提升最早启用的非系统账号
- **患者当前责任归属**：新增迁移 `6b7e2d4f9c10` 与 `patients.owner_id`；`created_by` 永久保留原创建者，归属转移只更新当前责任人并写审计
- **统一写权限**：所有账号可读取全部患者与事件；普通医生只可修改自己负责的患者，管理员可写全部；测试跳转同样先执行归属守卫，测试账号不能越权
- **管理员转移界面**：新增启用医生清单、管理员专用转移接口、责任医生展示、明确只读提示及二次确认；转移后原责任医生立即只读、新责任医生立即可写
- **阶段复评测量**：复评页默认带入最新身高体重，允许可选更新或留空沿用；BMI 统一重算，患者表保存最新值，事件载荷保存本次有效快照，不新增趋势表
- **患者列表**：姓名/住院号即时搜索；默认按逾期、今日、未来、无日期排序，同组最后编辑时间倒序；姓名、住院号、最后编辑、下次复评、当前环节均可双向排序，到期颜色在手工排序时仍保留
- **走查修复**：真实浏览器发现修改有效日期后列表未即时重排，已由 `8ea608e` 修复并加入回归测试
- **质量结果**：后端 **160 passed** + ruff；前端 **21 passed / 8 files** + lint + build；文档门禁 **3/3**；管理员 + 两位医生 + 两位分别归属患者的浏览器权限翻转与复评测量场景通过

### 新增（M6-A：依据数据与本地导入）

- **依据数据模型**：新增 `evidences` 与 `evidence_chunks`，材料使用稳定键与 SHA-256 追踪；只保存文件名，不记录本机绝对路径
- **迁移与索引**：新增迁移 `a1c9e7f2d4b8`；SQLite 支持时建立 FTS5 `trigram` 外部内容索引及同步触发器，不支持时保留普通表并允许 LIKE 降级
- **固定来源清单**：三份医学 PDF +《临床流程锁定稿》Markdown；默认清单锁定文件哈希以及 152 页/15 章节基线，材料变化时停止而非静默接受
- **抽取与分块**：PDF 不跨物理页，Markdown 不跨一级章节；只规范版式空白，不翻译、不总结、不生成临床建议
- **原子幂等导入**：新增 `scripts/import_evidence.py`；单份材料全部抽取成功后才替换，失败保留旧索引，重复运行不产生重复材料或分块
- **中文检索底座**：3 字及以上优先 FTS5 trigram，2 字或 FTS 不可用时走参数化 LIKE；双引号、FTS 操作符与 LIKE 通配符均按普通输入处理
- **真实材料结果**：三份 PDF 152/152 页、锁定稿 15/15 章节，共 684 个分块；重复导入全部跳过；开发库业务表迁移前后计数不变
- **质量结果**：后端 **168 passed** + ruff；文档门禁 **3/3**；迁移升级/降级/再升级、外键检查与中英文真实查询均通过

### 新增（M6-B：依据接口与隐私审计）

- **依据状态接口**：新增 `GET /api/evidence/status`，逐份返回四个批准来源的完整性，并明确区分“材料未完整导入”和“查询无命中”
- **检索接口**：新增 `GET /api/evidence/search`；检索词 2–100 字符、返回数 1–50，响应包含总命中、本次返回、材料名及 PDF 页码/锁定稿章节
- **完整性门禁**：材料哈希、标题、类型、文件名、PDF 页数、文本页数、元数据分块数和实际分块数共同决定是否可搜索，缺任一来源即阻断
- **权限边界**：状态和检索均要求登录；依据属于全局只读资料，管理员和启用普通医生均可查阅，不受患者责任归属限制
- **纯文本安全**：接口不返回 HTML 或绝对路径，片段最多保留局部上下文；不得生成面向患者的临床建议
- **隐私审计**：有效检索写 `evidence_search`，只记录查询字符数、检索模式、总命中和返回数量，不保存检索词与命中正文
- **质量结果**：后端 **176 passed** + ruff；开发库副本真实 HTTP 验证 4/4 材料就绪、“停药时间”命中锁定稿第九章，审计不含查询词

### 新增（M6-C：前端医学依据抽屉）

- **真实接口接入**：现有 `EvidenceDrawer` 接入状态与检索接口，工作台页头和侧栏继续共用同一抽屉
- **六类状态**：明确区分材料未导入、未输入、加载、无结果、接口错误和成功结果；网络/代理故障提供自然语言原因与重试动作
- **输入时序**：少于 2 字不请求；达到条件后延迟 300 ms 检索；新输入取消旧计时器与请求，避免旧结果覆盖
- **出处与安全**：每条显示材料名及 PDF 页码/锁定稿章节；React 纯文本渲染；固定显示“不替代医生判断”免责声明
- **无障碍**：打开自动聚焦检索框；Tab/Shift+Tab 不离开抽屉；Esc 可关闭；关闭后焦点返回原触发按钮
- **走查修复**：真实浏览器发现同一 PDF 页多条命中使用重复 React 键，已补充稳定序号并加入同页多命中回归测试
- **质量结果**：前端 **30 passed / 9 files**、lint、TypeScript 与 Vite build 全绿；隔离副本浏览器完成中文、英文数字混合、短词、无结果、故障恢复、页章出处与焦点验证

### 新增（M6-D：规则依据候选与 M6 收口）

- **36 条候选映射**：逐行填写依据性质、材料简称、PDF 物理页/锁定稿章节与关键结论，原文摘录默认留空
- **性质分层**：明确区分医学共识明确规则、共识倾向/原则、锁定稿或软件设计决定，以及非临床系统提示；不把软件流程强行归因于医学文献
- **复核边界**：36/36 行统一标记“待 Lifan/医学负责人复核”，没有任何行被自动写成已确认依据
- **可持续门禁**：`scripts/check_docs.py` 新增候选表结构、行数、ID、必填字段和复核状态检查，文档门禁升级为 **4/4**
- **材料核对**：使用本地全文索引定位并渲染相关物理 PDF 页核对；原 PDF、抽取全文、运行数据库及临时渲染均不进入仓库
- **M6 结论**：数据导入、接口审计、前端抽屉和资料候选四批工程工作完成；医学复核仍是后续人工 Gate，不因 M6 工程完成而自动关闭

### 新增（M0：规范基线）

- 建立 V1.0 工程骨架：`_DEV`（项目描述与甲方依据）、`_SPEC`（纲领性文件）、`backend`、`frontend`、`data`、`scripts`、`交付物`
- 归位甲方材料原件（临床流程锁定稿、学生首轮任务与联合交付要求、临床流程实现表）与医学依据材料（2 型糖尿病缓解中国专家共识、国际缓解共识、中国糖尿病防治指南 2024 版）
- 重写根目录 `UI.md`：以 Google Chrome 现行视觉体系（Material Design 3）取代原 macOS Liquid Glass 规范，并引入"Token 使用率自查"作为强制约束
- 建立根目录 `ACCESS.md`：记录 Agent 全部高权限行为，供开发者审计
- 编写 `_SPEC` 纲领：01 项目概述与目标边界 / 02 功能需求 / 03 业务规则与状态机 / 04 字段与输出模板 / 05 用户故事与验收标准 / 06 问题清单与裁决记录 / 07 技术架构与数据模型 / 08 实施计划与交付验收

### 变更

- 临床内核（9 状态、32 条规则、56 个字段、30 个 `OUT-*` + 2 个 `SYS-*` 锁定文案）自 V0.1 原样继承，医学内容不作任何改动
- 交付形态由"可点击低保真原型"升级为"完整成品"：便携版打包纳入验收项
- 依据来源以 `_DEV\甲方材料\原件` 与 `_DEV\医学依据` 为准（原件可信度高于工作区 md 派生版）
- UI 体系由 macOS Liquid Glass 改为 Google Chrome / Material Design 3；颜色 Token 已对照 Chromium 源码 GM3 参考调色板校准（15/18 逐字节一致）
- 删除零使用的 Token `--etmms-font-mono`（Token 使用率自查结果：60 个 Token 全部有使用点）

### 已知缺口

- 《规则映射表 v0.4》《原交接稿》未获取；依据核对以锁定稿 + 三份共识/指南 PDF 为准（记入 `_SPEC/06`）
- ~~打包工具对 Python 3.14 的支持需在 M1 实测~~ → M1 已实测通过（PyInstaller 6.22.3）

## [v0.1.0] — 2026-08-23（历史版本，已冻结）

见 `D:\LifanDataDeepSeek\ETMMS_V0.1_dev_260818\CHANGELOG.md`。
