"""
包名称：domain
所属层级：领域层（单一数据源）
功能说明：集中定义临床流程的"事实"——状态、合法转换、分支优先级、锁定输出文案、字段枚举。

为什么单独成层（`_SPEC/07` AD-01）：
    V0.1 的分支优先级藏在前端与后端的各自代码里，医生同时勾选互斥分支时会被静默覆盖。
    V1.0 把这些事实收敛到一处，并导出给前端（`GET /api/domain/*`），
    保证"文档、后端、前端"三者指向同一份定义。

模块划分：
    - states：9 个状态与中文名称、6 个流程单元。
    - enums：字段允许值（与最小字段映射 F001–F056 一致）。
    - templates：30 个 OUT-* + 2 个 SYS-* 锁定文案。
    - branches：阶段复评分支优先级表（含"被忽略分支必须给出原因"）。
    - transitions：合法状态转换表与校验函数。
"""
